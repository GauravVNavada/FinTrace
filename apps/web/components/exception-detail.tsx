"use client";

import Link from "next/link";
import * as React from "react";
import { ArrowLeft, ArrowRight, Check, FileText, Info, LockKeyhole, MoreHorizontal, Play, ShieldCheck, Sparkles, UserRound, X } from "lucide-react";
import { Badge, Button, Card, CardContent, CardHeader, CardTitle, Progress } from "@fintrace/ui";
import { exceptionDetails, formatCurrency } from "../lib/data";
import type { ExceptionDetail as ExceptionDetailType } from "../lib/types";
import { StatusBadge, SeverityBadge } from "./status-badge";

export function ExceptionDetail({ id }: { id: string }) {
  const exception = exceptionDetails[id] ?? exceptionDetails["EXC-1042"];
  const [investigated, setInvestigated] = React.useState(false);
  const [requested, setRequested] = React.useState(false);
  const investigation = exception.investigation;
  return <>
    <div className="mb-5 flex items-center gap-2 text-xs text-slate-400"><Link href="/exceptions" className="flex items-center gap-1 font-semibold text-slate-500 hover:text-slate-800"><ArrowLeft className="h-3.5 w-3.5" />Exceptions</Link><span>/</span><span>{exception.id}</span></div>
    <div className="mb-6 flex flex-col justify-between gap-4 lg:flex-row lg:items-start"><div><div className="mb-2 flex flex-wrap items-center gap-2"><span className="font-mono text-[11px] font-semibold text-slate-400">{exception.id}</span><SeverityBadge severity={exception.severity} /><StatusBadge status={requested ? "IN_REVIEW" : exception.status} /></div><h1 className="text-[26px] font-bold tracking-[-0.03em] text-slate-950">{exception.label}</h1><p className="mt-2 max-w-3xl text-sm text-slate-500">{exception.summary}</p></div><div className="flex items-center gap-2"><Button variant="outline" size="sm"><MoreHorizontal className="h-3.5 w-3.5" />More</Button><Button size="sm" onClick={() => setRequested(true)} disabled={requested}>{requested ? <><Check className="h-3.5 w-3.5" />Review requested</> : <><Play className="h-3.5 w-3.5" />Request review</>}</Button></div></div>
    <div className="mb-5 grid gap-4 sm:grid-cols-3"><Card><CardContent className="p-4"><div className="text-[11px] font-medium text-slate-400">Financial exposure</div><div className="mt-2 text-xl font-bold tracking-tight text-slate-950">{formatCurrency(exception.exposure)}</div><div className="mt-1 text-[11px] text-rose-600">Requires controlled review</div></CardContent></Card><Card><CardContent className="p-4"><div className="text-[11px] font-medium text-slate-400">Lifecycle</div><div className="mt-2 text-xl font-bold tracking-tight text-slate-950">{exception.orderId}</div><div className="mt-1 text-[11px] text-slate-500">{exception.ruleCount} deterministic rules triggered</div></CardContent></Card><Card><CardContent className="p-4"><div className="text-[11px] font-medium text-slate-400">Policy owner</div><div className="mt-2 text-xl font-bold tracking-tight text-slate-950">{exception.policy.owner}</div><div className="mt-1 text-[11px] text-slate-500">{exception.policy.state}</div></CardContent></Card></div>
    <div className="grid gap-5 xl:grid-cols-[minmax(0,1.25fr)_minmax(360px,.75fr)]"><div className="space-y-5"><Card><CardHeader className="flex-row items-center justify-between"><div><CardTitle>Transaction lifecycle</CardTitle><p className="mt-1 text-xs text-slate-400">Canonical records connected to {exception.orderId}</p></div><button className="flex items-center gap-1 text-xs font-semibold text-slate-500">View graph <ArrowRight className="h-3.5 w-3.5" /></button></CardHeader><CardContent><div className="grid gap-2 sm:grid-cols-2">{exception.lifecycle.map(record => <div key={`${record.source}-${record.id}`} className={`rounded-lg border p-3 ${record.status === "missing" ? "border-rose-200 bg-rose-50/40" : "border-slate-100 bg-slate-50/50"}`}><div className="flex items-center justify-between gap-2"><div className="flex items-center gap-2"><span className={`flex h-5 w-5 items-center justify-center rounded-full ${record.status === "confirmed" ? "bg-emerald-100 text-emerald-700" : record.status === "missing" ? "bg-rose-100 text-rose-600" : "bg-amber-100 text-amber-700"}`}>{record.status === "confirmed" ? <Check className="h-3 w-3" /> : record.status === "missing" ? <X className="h-3 w-3" /> : <Info className="h-3 w-3" />}</span><span className="text-xs font-semibold text-slate-800">{record.source}</span></div><span className="font-mono text-[10px] text-slate-400">{record.id}</span></div>{record.amount && <div className="mt-3 text-sm font-bold text-slate-900">{record.amount}</div>}<div className="mt-1 text-[11px] text-slate-500">{record.detail}</div></div>)}</div></CardContent></Card>
    <Card><CardHeader><CardTitle>Incident timeline</CardTitle><p className="mt-1 text-xs text-slate-400">Chronological activity across connected source systems</p></CardHeader><CardContent><div className="relative ml-2 border-l border-slate-200 pl-6">{exception.timeline.map(event => <div key={`${event.time}-${event.title}`} className="relative pb-6 last:pb-0"><span className={`absolute -left-[31px] top-0.5 flex h-4 w-4 items-center justify-center rounded-full border-2 border-white ${event.state === "complete" ? "bg-emerald-500" : event.state === "warning" ? "bg-amber-400" : "bg-rose-500"}`}>{event.state === "complete" && <Check className="h-2.5 w-2.5 text-white" />}</span><div className="flex flex-col gap-1 sm:flex-row sm:items-start sm:justify-between"><div><div className="text-xs font-semibold text-slate-800">{event.title}</div><div className="mt-1 text-[11px] text-slate-500">{event.detail}</div></div><div className="flex items-center gap-2 text-[10px] text-slate-400"><span>{event.source}</span><span className="font-mono">{event.time}</span></div></div></div>)}</div></CardContent></Card>
    <Card><CardHeader className="flex-row items-center justify-between"><div><CardTitle>Audit history</CardTitle><p className="mt-1 text-xs text-slate-400">Append-only record of actions on this exception</p></div><ShieldCheck className="h-4 w-4 text-emerald-600" /></CardHeader><CardContent className="p-0"><div className="divide-y divide-slate-100">{exception.audit.map(event => <div key={`${event.time}-${event.action}`} className="flex items-center gap-3 px-5 py-3.5"><div className="flex h-7 w-7 items-center justify-center rounded-full bg-slate-100"><FileText className="h-3.5 w-3.5 text-slate-500" /></div><div className="min-w-0 flex-1"><div className="text-xs font-semibold text-slate-800">{event.action}</div><div className="mt-0.5 truncate text-[11px] text-slate-400">{event.actor} · {event.detail}</div></div><span className="font-mono text-[10px] text-slate-400">{event.time}</span></div>)}</div></CardContent></Card></div><div className="space-y-5"><Card className="border-slate-300"><CardHeader className="border-b border-slate-100 bg-slate-50/50"><div className="flex items-center gap-2"><div className="flex h-7 w-7 items-center justify-center rounded-md bg-violet-100 text-violet-700"><Sparkles className="h-4 w-4" /></div><div><CardTitle>Evidence investigation</CardTitle><p className="mt-1 text-xs text-slate-400">Bounded, read-only AI analysis</p></div></div></CardHeader><CardContent className="p-5">{!investigated ? <div><div className="rounded-lg border border-violet-100 bg-violet-50/50 p-4"><div className="flex gap-3"><LockKeyhole className="mt-0.5 h-4 w-4 shrink-0 text-violet-600" /><p className="text-xs leading-5 text-slate-600">FinTrace will inspect only relevant, organization-scoped evidence using six approved read-only tools. No financial action will be taken.</p></div></div><Button className="mt-4 w-full" onClick={() => setInvestigated(true)}><Sparkles className="h-3.5 w-3.5" />Investigate exception</Button><div className="mt-3 text-center text-[10px] text-slate-400">Typical investigation time · under 3 seconds</div></div> : <InvestigationResult investigation={investigation} />}</CardContent></Card><Card><CardHeader className="flex-row items-center justify-between"><div><CardTitle>Approval guardrail</CardTitle><p className="mt-1 text-xs text-slate-400">Why this incident cannot auto-resolve</p></div><LockKeyhole className="h-4 w-4 text-slate-400" /></CardHeader><CardContent><div className="flex items-start gap-3"><div className="mt-0.5 h-2 w-2 rounded-full bg-amber-400" /><div><div className="text-xs font-semibold text-slate-800">{exception.policy.state}</div><p className="mt-1 text-[11px] leading-5 text-slate-500">{exception.policy.reason}. The recommended action is simulated and requires a {exception.policy.owner.toLowerCase()} decision.</p></div></div><div className="mt-4 flex items-center gap-2 border-t border-slate-100 pt-4 text-[11px] text-slate-400"><UserRound className="h-3.5 w-3.5" />Current actor: Aarav Mehta · Analyst</div></CardContent></Card><Card><CardHeader className="flex-row items-center justify-between"><div><CardTitle>Similar incidents</CardTitle><p className="mt-1 text-xs text-slate-400">Linked by rule and workflow</p></div><Badge className="bg-slate-100 text-slate-600">12 found</Badge></CardHeader><CardContent><div className="flex items-center justify-between"><div><div className="text-lg font-bold text-slate-900">₹71,420</div><div className="mt-1 text-[11px] text-slate-500">associated exposure this month</div></div><div className="h-12 w-24"><div className="flex h-full items-end gap-1">{[20, 28, 24, 40, 36, 56, 48, 70, 64, 86].map((height, index) => <span key={index} className="flex-1 rounded-t-sm bg-rose-200" style={{ height: `${height}%` }} />)}</div></div></div><Link href="/patterns" className="mt-4 flex items-center justify-between border-t border-slate-100 pt-3 text-xs font-semibold text-slate-700">Review pattern <ArrowRight className="h-3.5 w-3.5" /></Link></CardContent></Card></div></div>
  </>;
}

function InvestigationResult({ investigation }: { investigation: ExceptionDetailType["investigation"] }) {
  return <div><div className="flex items-center justify-between"><div className="flex items-center gap-2"><span className="flex h-6 w-6 items-center justify-center rounded-full bg-emerald-100 text-emerald-700"><Check className="h-3.5 w-3.5" /></span><span className="text-xs font-bold text-emerald-700">{investigation.status}</span></div><span className="text-[10px] font-semibold uppercase tracking-wide text-slate-400">Verified result</span></div><div className="mt-4 text-sm font-bold text-slate-900">{investigation.rootCause}</div><p className="mt-2 text-xs leading-5 text-slate-500">{investigation.summary}</p><div className="mt-5 rounded-lg border border-slate-100 p-3"><div className="flex items-center justify-between"><span className="text-[11px] font-semibold text-slate-600">Evidence score</span><span className="text-sm font-bold text-slate-900">{investigation.evidenceScore}<span className="text-[11px] font-medium text-slate-400">/100</span></span></div><Progress value={investigation.evidenceScore} className="mt-2" /><div className="mt-2 text-[10px] text-slate-400">Strong evidence · not an AI confidence score</div></div><div className="mt-5"><div className="mb-2 text-[10px] font-bold uppercase tracking-[0.14em] text-slate-400">Supporting evidence</div><div className="space-y-2">{investigation.supportingEvidence.map(item => <div key={`${item.source}-${item.fact}`} className="flex gap-2 text-[11px] leading-4"><span className={`mt-1 h-1.5 w-1.5 shrink-0 rounded-full ${item.tone === "positive" ? "bg-emerald-500" : item.tone === "warning" ? "bg-rose-500" : "bg-slate-400"}`} /><span className="text-slate-600"><strong className="font-semibold text-slate-800">{item.source}:</strong> {item.fact}</span></div>)}</div></div><div className="mt-5 rounded-lg bg-indigo-50 p-3"><div className="flex items-center gap-1.5 text-[10px] font-bold uppercase tracking-wide text-indigo-700"><ArrowRight className="h-3.5 w-3.5" />Recommended next action</div><div className="mt-1.5 text-xs font-semibold text-indigo-950">{investigation.action}</div><div className="mt-2 text-[10px] leading-4 text-indigo-700">Human review required. No resolution performed.</div></div><div className="mt-5 border-t border-slate-100 pt-4"><div className="mb-2 flex items-center justify-between text-[10px] font-bold uppercase tracking-[0.14em] text-slate-400"><span>Read-only tool calls</span><span>{investigation.tools.length} tools</span></div><div className="space-y-1.5">{investigation.tools.map(tool => <div key={tool.name} className="flex items-center justify-between rounded bg-slate-50 px-2.5 py-2 font-mono text-[10px]"><span className="text-slate-600">{tool.name}<span className="text-slate-400">({tool.target})</span></span><span className="text-slate-400">{tool.duration}</span></div>)}</div></div></div>;
}
