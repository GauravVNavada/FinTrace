"use client";

import * as React from "react";
import type { LucideIcon } from "lucide-react";
import { CircleHelp, ClipboardCheck, FileBarChart, Home, LogOut, MessageCircleQuestion, PanelLeft, Rows3 } from "lucide-react";
import Image from "next/image";
import Link from "next/link";
import { usePathname, useRouter } from "next/navigation";
import { Button, cn } from "@fintrace/ui";
import { appConfig } from "../lib/data";
import { fetchReadiness, getClientIdentity } from "../lib/api-client";

type NavItem = { label: string; href: string; icon: LucideIcon };
const primaryNav: NavItem[] = [{ label: "Home", href: "/", icon: Home }, { label: "Closes", href: "/investigations", icon: Rows3 }, { label: "Attention", href: "/attention", icon: ClipboardCheck }];
const secondaryNav: NavItem[] = [{ label: "Audit", href: "/audit", icon: FileBarChart }, { label: "Evaluation", href: "/evaluations", icon: FileBarChart }];

function Nav({ items, mobile = false }: { items: NavItem[]; mobile?: boolean }) {
  const pathname = usePathname();
  return <nav aria-label={mobile ? "Mobile navigation" : "Primary navigation"} className={mobile ? "flex gap-1 overflow-x-auto px-4 py-2 lg:hidden" : "space-y-1"}>{items.map(item => { const Icon = item.icon; const active = item.href === "/" ? pathname === "/" : pathname.startsWith(item.href); return <Link key={item.href} href={item.href} aria-current={active ? "page" : undefined} className={cn("flex items-center gap-3 rounded-lg text-[13px] font-medium transition-colors", mobile ? "whitespace-nowrap px-3 py-2" : "px-3 py-2.5", active ? "bg-sidebar-accent text-sidebar-foreground" : "text-sidebar-muted hover:bg-sidebar-accent/70 hover:text-sidebar-foreground")}><Icon className={cn("h-4 w-4", active ? "text-accent" : "text-sidebar-muted")} />{item.label}</Link>; })}</nav>;
}

export function AppShell({ children }: { children: React.ReactNode }) {
  const router = useRouter(); const pathname = usePathname(); const [identity, setIdentity] = React.useState(getClientIdentity); const [apiState, setApiState] = React.useState<"checking" | "connected" | "unavailable">("checking"); const [helpOpen, setHelpOpen] = React.useState(false);
  React.useEffect(() => { if (!pathname.startsWith("/login") && !window.localStorage.getItem("fintrace.access_token")) { router.replace("/login"); return; } setIdentity(getClientIdentity()); fetchReadiness().then(() => setApiState("connected")).catch(() => setApiState("unavailable")); }, [pathname, router]);
  function logout() { window.localStorage.removeItem("fintrace.access_token"); window.localStorage.removeItem("fintrace.identity"); router.replace("/login"); }
  const apiLabel = apiState === "connected" ? "Connected" : apiState === "unavailable" ? "Unavailable" : "Connecting"; const apiTone = apiState === "connected" ? "bg-success" : apiState === "unavailable" ? "bg-destructive" : "bg-warning";
  return <div data-theme="operations" className="flex min-h-screen bg-background"><a href="#main-content" className="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-50 focus:rounded-md focus:bg-card focus:px-3 focus:py-2 focus:text-xs focus:font-semibold focus:text-foreground focus:shadow-lg">Skip to main content</a><aside className="fixed inset-y-0 left-0 z-20 hidden w-[232px] flex-col bg-sidebar text-sidebar-foreground lg:flex"><div className="flex h-[82px] items-center border-b border-sidebar-border px-5"><Image src="/fintrace-mark.png" alt="" aria-hidden="true" width={30} height={30} className="rounded-lg object-cover" priority /><div className="ml-3"><div className="text-[15px] font-bold tracking-tight">{appConfig.productName}</div><div className="mt-0.5 text-[10px] uppercase tracking-[0.16em] text-sidebar-muted">Financial close control</div></div></div><div className="px-4 py-5"><div className="mb-3 px-3 text-[10px] font-bold uppercase tracking-[0.16em] text-sidebar-muted">Workspace</div><Nav items={primaryNav} /></div><div className="px-4"><div className="mb-3 px-3 text-[10px] font-bold uppercase tracking-[0.16em] text-sidebar-muted">More</div><Nav items={secondaryNav} /></div><div className="mt-auto border-t border-sidebar-border px-4 py-4"><Button type="button" variant="ghost" className="w-full justify-start gap-3 px-3 py-2.5 text-left text-[13px] font-medium text-sidebar-muted hover:bg-sidebar-accent/70 hover:text-sidebar-foreground" onClick={() => setHelpOpen(value => !value)}><MessageCircleQuestion className="h-4 w-4" />Help</Button>{helpOpen && <div className="mt-3 rounded-lg border border-sidebar-border bg-sidebar-accent/70 p-3 text-[11px] leading-5 text-sidebar-muted">Upload your period exports, run the close, then work only the items in Attention.</div>}</div></aside><div className="flex min-h-screen min-w-0 flex-1 flex-col lg:pl-[232px]"><header className="sticky top-0 z-10 border-b border-border bg-card/95 backdrop-blur"><div className="flex h-[70px] items-center justify-between px-4 sm:px-6 lg:px-9"><div className="flex items-center gap-3"><PanelLeft className="h-4 w-4 text-muted-foreground lg:hidden" /><span className="text-xs font-medium text-muted-foreground">{appConfig.productName} <span className="mx-1 text-border">/</span> Financial close</span></div><div className="flex items-center gap-3"><div className="hidden items-center gap-2 text-xs text-muted-foreground sm:flex"><span className={cn("h-2 w-2 rounded-full", apiTone)} />{apiLabel}</div><span className="hidden text-xs font-semibold text-foreground sm:inline">{identity.display_name}</span><Button variant="outline" size="sm" className="gap-2" onClick={logout}><LogOut className="h-3.5 w-3.5" /><span className="hidden sm:inline">Log out</span></Button><Button variant="ghost" size="icon" aria-label="Help" onClick={() => setHelpOpen(value => !value)}><CircleHelp className="h-[18px] w-[18px] text-muted-foreground" /></Button></div></div><Nav items={[...primaryNav, ...secondaryNav]} mobile /></header><main id="main-content" tabIndex={-1} className="flex-1 px-4 py-7 outline-none sm:px-6 lg:px-9 lg:py-9">{children}</main><footer className="border-t border-border px-4 py-4 text-[11px] text-muted-foreground">{appConfig.productName} · {appConfig.workspaceName}</footer></div></div>;
}
